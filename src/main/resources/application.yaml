spring:
  application:
    name: meetingease

  liquibase:
    enabled: true
  
  datasource:
    url: jdbc:postgresql://localhost:5432/meeting_ease
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none